Generate a pre-meeting brief for the following client.

=== CRM CONTEXT ===
{crmContextJson}

=== CHANGE REPORT (since last meeting {lastMeetingDate}) ===
{changeReportJson}

Respond with ONLY the JSON brief object. No text outside the JSON.

----------------------------------------------------------------------
Placeholders (filled at runtime by SynthesisService):
  {crmContextJson}   — pretty-printed CRM context (name, segment, tenure,
                       last meeting notes, open promises, synthetic products)
  {lastMeetingDate}  — the date deltas were computed against
  {changeReportJson} — pretty-printed deterministic ChangeReport (account
                       summaries, large outflows, maturity alerts, dormancy).
                       This is the ONLY numeric source the model may cite.
